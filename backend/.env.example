PORT=3000
NODE_ENV=production
APP_URL=https://your-domain.com

JWT_SECRET=          # openssl rand -hex 32
ENCRYPTION_KEY=      # openssl rand -hex 16

DB_USER=crmuser
DB_PASSWORD=         # your password
DB_NAME=zalocrm
DATABASE_URL=postgresql://crmuser:password@db:5432/zalocrm

UPLOAD_DIR=/var/lib/zalo-crm/files

# Feature 0036 — AI reply suggestions (BYOK).
# 32 bytes hex (64 chars). Master key used to derive per-org AES-256-GCM keys
# that encrypt provider API keys in the database. NEVER commit a real value.
# Generate: openssl rand -hex 32
AI_CONFIG_MASTER_KEY=    # openssl rand -hex 32
